# The screen's specs open on their chronicle, and the stand-in goes

**Line:** The screen's specs open on their chronicle, and the stand-in goes — hover, reference, browse, menu, controls and window, reviewed the same way; then the helpers that settle through the screen, their headless twin and the stand-in catalogue are deleted, boot, resume and failed-boot staying on the address they test. Done when no file under `e2e/` or `src/` imports `src/content/stand-in.ts`, that file and `src/content/stand-in.test.ts` are deleted, `catalogueOf` in `src/content/catalogues.ts` resolves the ages' catalogues alone, the shared module holds no helper that settles through the screen and no search on the stand-in, the run skill and the ui-check agent name a Nomadic address, the report carries the eight specs' duration before and after; CI is green.

**Spec:** `DOGMAS.md` → _Testing_, the rule **A spec opens on the save it wrote, on the game's content**, read whole — its sentence _a spec that tests the boot, the address or the save's own reading opens the way it tests_ is what boot's tests, resume's round trip and the two tests on the chronicle's start open under, and its sentence _a mechanism no age's content reaches has no spec_ is what takes the stack of twelve and the content the page does not list out of the suite — and the rule **A spec rests before it presses or measures what just changed**. The sentences the tests assert are `docs/INTERFACE.md` → _The menu_, _The launch page_, _The presses_, _A card's names and its label_ and _What stands over what_, and `docs/CHRONICLE-SCREEN.md` → _The windows_ and _The resource bar_; the content they read is `docs/ages/NOMADIC.md` → _The settle_, _The cards_ and _The events_, unchanged. One `docs/` sentence changes, in `docs/INTERFACE.md` → _The launch page_: the sentence _The rows list the ages' content alone; content only the address names is listed beside it while the page stands._ becomes _The rows list the ages' content alone._ Three sentences outside `docs/` change in the same commit, written out here so none is authored: the layout table at the foot of `DOGMAS.md` → _Stack_, its `src/content/` row, reads _the catalogues: the ages' content; what the boot hands the rules and the screen_; `.claude/skills/run/SKILL.md` step 5, its last sentence, reads _Naming a deck opens a chronicle straight and writes it over the save, the deck read from the content the address names or the default (`?deck=nomadic`)._; `.claude/agents/ui-check.md` → _What the app is_, its parenthesis, reads _`?content=` the catalogue the deck is read from, the default where it is left out_, and the skeleton's `url` is `http://localhost:5173/?deck=nomadic`. Player-facing sentences: none.

**Doc-impact:** `docs/INTERFACE.md` → _The launch page_, the one sentence above.

**Scope:**

- In: the six specs the line names, `e2e/boot.spec.ts` and `e2e/resume.spec.ts` for their fixtures, `e2e/chronicle-screen.ts`, `e2e/press.spec.ts`, `e2e/deal.spec.ts` and `e2e/fall.spec.ts` where a helper they hold is shared out, the stand-in catalogue and its test, `src/content/catalogues.ts`, the launch page's dead branch, the three harness sentences and the one `docs/` sentence. Out: every other spec, `e2e/failed-boot.spec.ts` untouched, `CHANGELOG.md` untouched, being history.
- **What goes from the shared module**: the on-screen settle and the free claim, the address opener on the stand-in, the headless twin `launch` and its border, the raid-deal search, the worker-run search with what only it calls, the fall search, and the stand-in imports. The address opener on a catalogue, the save opener, the seed search, the bare fixture, the stepped-worker search, the headless turn and the first-entries take, the turn budget, the end-turn and stopped-turn presses all keep callers and stay.
- **What moves into the shared module**, each used by two files from here on: the lean-season search from the deal spec, seed 3's first deal standing alone and offering Share; the fall search from the fall spec, and the defeat screen's reading beside the victory's; the tile-aimed-playable search from the press spec; the bare-tile search from the city-mode spec; the doubled deck from the press spec, now the Nomadic deck's cards twice with its settle section as it is, the press spec taking the shared one. What a single file uses stays in that file.
- **Seven fixtures**, each on the Nomadic catalogue, the address ones on seed 1:
  - _Before the settle_: the chronicle as launched on the first region, schedule and deck, turn 0, the hand the deck's settle section — settle, first worker, first scout. Reached on the address through the shared address opener, never planted; the capstone's window closed by its card.
  - _Bare turn 1_: the shared bare fixture on seed 1, the settle alone and the phase ended. Measured: the hand gather, worker, gather, gather, warrior; twelve in the draw pile, five gathers among them, so one of the first six laid out differs from the first; the first deal due on turn 6; the tile three up from the centre, `0,-3`, is coast, charted, with nothing on it; the bare-tile search two out answers `-2,0`, coast.
  - _Aimable_: the tile-aimed-playable search on bare turn 1. Measured: seed 1, gather at the first place in the hand; the aim's catcher rises for it on screen, as the press spec walks it.
  - _First deal_: bare turn 1 with turns ended headlessly through the shared headless turn until the turn before the timeline's first deal, and the end of turn applied so the deal stands. Measured: turn 6, the departure, offering _Let them go_ then _Keep them_, seven units, no ending.
  - _Lean season_: the shared lean-season search. Measured: seed 3, turn 6, Share first among the answers and Ration second; Share's rules entry names Hunger, and Hunger's names nothing.
  - _Fallen_: the chronicle the shared fall search answers with the end of turn applied through `apply`, so it stands ended. Measured: seed 1, standing on turn 39, the capture at its end, found in a quarter of a second.
  - _Piles overflowing_: the shared doubled deck, settled bare on seed 1 and three turns ended headlessly. Measured: turn 4, fourteen in the draw pile and fifteen in the discard pile, no deal, no ending, five units; the frame holds twelve at six a row, so both piles scroll. At turn 1 on the same deck the draw pile holds twenty-nine, which is what the press spec's browse test needs.
- **Hover**, fourteen tests, in file order: every test but the two on the deal opens on _bare turn 1_ through the save opener; the tile inspected under the panel-row tests is the bare-tile search's answer, and the card whose kind label is read takes its kind from the catalogue of the chronicle's content. The reading hovered while the deal window stands, and the answer's kind label on the deal window, open on _first deal_: the save opener closes the capstone's window and the deal's rises under it. The viewport, the rest past a tooltip's and the hand cursor stay.
- **Reference keeps two tests.** The first opens on _lean season_ and keeps every step the content reaches, on Share's card at the place the rules' offer lists it: the name resting shows Hunger small; the pointer on the Menu button takes it down and reads a hand; back on the name raises it; onto the small card keeps it up with a hand; beside the deal takes it down; a right click on the small card shows Hunger large with the small card gone, the back key takes it down and the deal stands; a right click on the name shows Hunger large alone, the back key takes it down; a right click on Share's card shows Share large; over its body the cursor is an arrow and over its name a hand; a right click on the name stands Hunger over Share, and the card beneath peeks out up and to the left, measured on the two faces' centres and never on their names, since the two faces are different cards now; the back key takes Hunger down and leaves Share, the back key again takes Share down and the deal stands. Its title says what it now proves and names no twelve. The loop to twelve, the right click that adds nothing once twelve stand, the small card raised from a small card's name and the copy added of a card already standing all go: no Nomadic card names a card that names anything. The second, the building named on the settle card, opens on _before the settle_: the first card of the hand is the deck's settle section's first, read from the catalogue; the named thing is the building the catalogue's city names, `{ kind: 'building', id }`; every step as it is.
- **Browse keeps three tests.** The scroll test opens on _piles overflowing_: the draw pile's browse scrolls, stops on its first and last row, and takes the drag, the back key closes it, then the discard pile's browse straight after, nothing ended on screen. The selection test opens on _bare turn 1_, the second card differing from the first found among the first six laid out, as now. The moving small-card-and-bubble test opens on _piles overflowing_, the draw pile's browse, as it is.
- **Menu keeps seven tests.** The walk into Controls, Escape on a bare screen, the pan onto the Menu button and the selected tile waiting open on _bare turn 1_ as they are. The card let go of before the menu opens on _aimable_: the card dragged out and aimed, the back key drops the aim with no menu and the hand equal to the fixture's, the back key again raises the menu; no worker entered, no unit moved. The new chronicle opens on _bare turn 1_: New chronicle pressed, the fresh chronicle on turn 0 with another seed, its cards across the three piles equal to the cards and the settle section of the deck the save names, read from the catalogue; the assertion that the played chronicle stands past turn 1 goes with the turns. The menu over the defeat screen opens on _fallen_ planted directly, the defeat screen standing and no capstone's window, then the menu, New chronicle, and the fresh chronicle as now.
- **Controls**, thirteen tests, every one on _bare turn 1_ through the save opener, the gestures as they are. The bare tile stays the literal three up from the centre with its comment re-shaved to what it is on the fixture: charted, nothing on it, clear of the bar, the piles and the hand. The reload test's comment says what now happens: the bare address resumes the save the screen wrote, and the capstone's window a resumed chronicle opens under waited under the menu.
- **Window**, one test, on _bare turn 1_ at its small viewport.
- **Resume keeps four tests.** The round trip opens on _before the settle_ on the address, closes the capstone's window, plays the settle on the centre tile on screen — the first card dragged out and aimed, the tile pressed, the play-out waited for, the city standing — reads the chronicle, goes to the bare address, finds the capstone's window standing, closes it and reads the same chronicle. The unreadable save stays as it is. The ended chronicle opens on _fallen_: the defeat screen shown, no capstone's window, the chronicle equal to the fixture. The deal waiting opens on _first deal_: the capstone's window standing and the deal's not, the close, the deal's window with the departure's name as its title, the chronicle equal to the fixture.
- **Boot keeps five tests.** The address naming a deck names the Nomadic version and the first deck the catalogue lists, read from it. The test on content the page does not list is deleted, with the launch page's branch that listed such content beside the ages' and the lookup's fallback to the stand-in; the lookup's comment says it resolves the ages' catalogues alone. The other four stay as they are.
- **The oracle**: after a gesture, the chronicle on screen equals the rules' outcome of the same command on the fixture, or the fixture itself where the gesture plays nothing, and the screen facts stay as they were. No literal names a card, a deck, an event or an answer: each is read from the catalogue, the deck's sections, the rules' offer or the chronicle. None of the eight specs ends a turn on screen; the turn budget stays only where the settle's play-out is counted, as the settle spec counts it.

**Traps:**

- The boot reads the save only on a bare address (`src/main.ts`): the save opener goes to `/`, never to an address naming content or a seed. The save is planted through `addInitScript` before the navigation; the entry key is `SAVE_ENTRY` in `src/ui/save-entry.ts`. The save opener polls for the capstone's window and closes it; an ended chronicle raises none (`src/ui/overlay.ts`), so the fallen fixture opens through the plant and the bare address without that wait, as the resume spec's own opener does today — how the opener takes that is the implementer's call. A chronicle saved with a deal standing opens on that deal's window once the capstone's is closed.
- The address opener names content, seed, deck and schedule; the region defaults to the catalogue's first; the boot opens a chronicle on an address only where it names a deck. The address opener leaves the capstone's window standing; `capstoneClosed` closes it by its card, since the back key is rebindable and specs rebind it.
- The rules' offer, `offered` in `src/rules/schedule.ts`, gives the answers in the order the deal window lays them; the place of an answer is read from it, never assumed. On the departure, both answers cost nothing on turn 6 with a population of one, so the first-entries take is never refused before the fixture stops.
- A rules helper called outside `apply` answers a stage, `{ chronicle }`, not a chronicle, and charts nothing; every fixture here is reached through `apply` and the ended turns alone, so nothing needs `charted`. `writeSave` validates by reading back, and a fixture the reading refuses throws at the plant, which is the finding.
- The doubled deck is a launch choice, not a chronicle edit: the deck is handed to the launch as the press spec hands its own today, and the save keeps the catalogue's deck id beside the chronicle. The browse frame holds two rows of six at the browse's top, `layGrid` in `src/ui/overlay.ts`; a third row overflows.
- A card's kind label carries the hand cursor and raises the one bubble of its surface, `tooltip-ui` in the hand and `tooltip-overlay` on a window (`src/ui/tooltip.ts`, `src/ui/overlay.ts`). A small card is named `small-card-<n>`, the card shown large `inspection` and the ones beneath it `inspection-<n>` from the bottom; each carries its card's id or its reference in its data, read through `cardOnFace` and `referenceOnFace`.
- The stack's band, `BAND` in `src/ui/overlay.ts`, shifts each card beneath the newest up and to the left by a fixed design length; the faces' centres measure it, and a name's place is the face's own layout.
- Only the newest card's names answer a press; Hunger's rules entry names nothing on the Nomadic content (`src/ui/text.ts`), so no third card can join the stack from the lean-season deal, and no small card raised there carries a name.
- The Nomadic settle card names the city's building, `rules.settle` in `src/ui/text.ts`; the settle section's first card is the settle, and the launch deals the settle section as the hand on turn 0.
- The reload in the controls spec keeps `localStorage` and the init scripts: the boot resumes the save the chronicle screen wrote, the Controls key it kept beside it, and `window.named` is installed again. The capstone's window rises on the resumed chronicle and waits under the menu; the chord and held-key readings need it closed first, as the spec closes it today.
- The bare tile three up from the centre is charted on the Nomadic content, the centre part being the 37 tiles within three, and it is coast on seed 1 with nothing on it: a middle click or a drag on it that rings nothing is the stronger test the stand-in's uncharted tile never made. The map's own spec keeps the same literal.
- The menu's New chronicle deals the choices the save keeps beside the chronicle, region and deck, with a fresh seed (`src/main.ts`, `src/ui/menu-scene.ts`); the oracle is the deck the save names, read through `deckOf` on the catalogue, its cards and its settle section together.
- The launch page's `listed` array in `src/ui/launch-page.ts` pushes the chosen catalogue's version where the ages' list lacks it; once `catalogueOf` refuses any version outside `CATALOGUES`, that push never runs and goes. `firstsOf` in `src/ui/launch-page.ts` and the shared module's `firstsOf` are two helpers of one name; the shared one stays the specs'.
- `src/rules/fixtures.ts` holds the rules tests' own fixture catalogue, which is not the stand-in; `settledLaunch` there keeps its callers in `src/content/nomadic.test.ts` and `src/content/scripts.test.ts`. `src/rules/map.test.ts` says "stand-in" of a fixture in a comment and imports nothing.
- CI runs the suite split by spec across four runners; press is the slowest spec and sets the slowest shard on its own. None of the eight specs here comes near it.
- A spec's duration is measured with nothing else running on the machine; a run beside the user's play reads contention, not the spec. Measured today, one spec at a time, all green: hover 1.8 min (fourteen tests), reference 32 s (two), browse 35 s (three), menu 1.4 min (seven), controls 1.6 min (thirteen), window 5 s (one), resume 14 s (four), boot 9 s (six).
- The named-spec hook refuses a Playwright run that names no spec. `docs/PHASER.md` → _Under a Playwright spec_ says why a spec rests after a load, a scene start and a raise, and why a press in the frame an object appears falls through it.

**Plan:**

1. The shared module: the lean-season, fall, tile-aimed-playable and bare-tile searches, the defeat screen's reading and the doubled deck moved in from the specs that hold them, the press, deal, fall and city-mode specs reading them from there; the fallen chronicle's opener. Leaves standing: `npm run check` and `npm run lint` green, the four specs that lost a helper green on their own runs, each named in the report with the question it answered.
2. `e2e/window.spec.ts` and `e2e/controls.spec.ts` on _bare turn 1_. Leaves standing: each green on its own run, named in the report.
3. `e2e/hover.spec.ts`, its twelve tests on _bare turn 1_ and its two on _first deal_. Leaves standing: green on its own run, named in the report.
4. `e2e/menu.spec.ts`, its seven tests on their fixtures. Leaves standing: green on its own run, named in the report.
5. `e2e/browse.spec.ts`, its three tests on their two fixtures. Leaves standing: green on its own run, named in the report.
6. `e2e/reference.spec.ts`, its two tests, the first cut to what the content reaches. Leaves standing: green on its own run, named in the report.
7. `e2e/resume.spec.ts` and `e2e/boot.spec.ts`, their fixtures Nomadic, boot's fifth test deleted. Leaves standing: each green on its own run, named in the report.
8. The stand-in's deletion: the six helpers and the imports out of the shared module, `src/content/stand-in.ts` and its test deleted, `catalogueOf` on the ages alone, the launch page's dead branch gone; the one `docs/` sentence, the `DOGMAS.md` row, the run skill's and the ui-check agent's sentences as written above. Leaves standing: `npm run check`, `npm run lint`, `npm test` green, and no import of the stand-in anywhere, checked by a search.
9. Time the eight again the same way, and put both readings in the report as one table.
10. The board line deleted.

**Verify:** `npm run check`, `npm run lint`, `npm test`, and a search over `e2e/` and `src/` for `stand-in` finding the map test's comment alone. The proof spec is `npx playwright test e2e/menu.spec.ts`: it walks the save opener, the aimable search, the fallen chronicle's opener, the new-chronicle oracle and the menu over both screens. CI proves on the push: `hover`, `reference`, `browse`, `controls`, `window`, `resume`, `boot`, `press`, `deal`, `fall`, `city-mode`, and every other spec, since the shared module changed. The timing table: the eight specs run once alone before step 1, the readings above, and once after step 8, on an idle machine, durations as Playwright's list reporter prints them, both columns in the report, the deleted tests on their own rows.
