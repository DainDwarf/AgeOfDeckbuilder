# The city's specs open on their chronicle

**Line:** The city's specs open on their chronicle — city-mode, yields, settle and capstone, reviewed the same way: each test opened on the state it needs, its oracle read from the rules; a test on the chronicle's start opens a new chronicle on the address, as a player starts one, and every other opens on a save built headlessly on the Nomadic content from a seed through the rules' own helpers. Done when `e2e/city-mode.spec.ts`, `e2e/yields.spec.ts`, `e2e/settle.spec.ts` and `e2e/capstone.spec.ts` import nothing from `src/content/stand-in.ts` and open on the Nomadic content, through the address or through the shared save opener; the report carries the four specs' duration before and after; CI is green.

**Spec:** `DOGMAS.md` → _Testing_, the rule **A spec opens on the save it wrote, on the game's content**, read whole — its sentence _a spec that tests the boot, the address or the save's own reading opens the way it tests_ is what the four tests on the chronicle's start open under — and the rule **A spec rests before it presses or measures what just changed**. The sentences the tests assert are `docs/CHRONICLE-SCREEN.md` → _The settle phase_, _City mode_, _The windows_ (the capstone's window), _The resource bar_ and _The yield overlay_, unchanged; the content they read is `docs/ages/NOMADIC.md` → _The settle_, _The land_ and _The capstone_, unchanged. No `docs/` sentence is added or changed. Player-facing sentences: none.

**Doc-impact:** none — the line changes the e2e suite alone; the rule it executes stands in `DOGMAS.md`, and no `docs/` page describes the suite.

**Scope:**

- In: the four specs, and `e2e/chronicle-screen.ts` where a Nomadic fixture, a search or an opener is shared. Out: every other spec, the stand-in catalogue, and the stand-in's helpers in the shared module — `open`, `settle`, `launch`, `openOnCapstone`, `workerRun`, `dealRun`, `fallRun`, `unaffordableRun`, `atTileRun` — every one of which keeps callers in the screen's specs and dies with the last of them. `stepRun` and what only it calls lose their last caller here and go now. `glyphsOf`, `drawnFaces`, `glyphs` and `noGlyphs` read no content and stay.
- **Two ways to open, by what the test is about.** A test on the chronicle's start — the settle phase, the settle card, the opening's capstone window — opens a new chronicle on the address naming the Nomadic content, seed 1 and the first region, schedule and deck the catalogue lists, the way `openOnCapstone` opens the stand-in today and the way a player starts one; it then plays the settle phase on screen where the test asks for it, that being the start it tests. Every other test opens on a save planted through the shared save opener, and plays on screen the gesture it tests and nothing before it. Four tests open on the address: city-mode's first, the settle spec's two and the capstone spec's first. When the launch screen lands, the four open through it and never through Continue.
- **No seed search where a rules helper builds the state.** Every fixture opens on seed 1; a tile a test needs is read off the fixture — the first tile the city may claim, the nearest uncharted tile, a bare charted tile two out — never a literal.
- **Seven fixtures**, each built on the Nomadic catalogue from seed 1:
  - _Before the settle_: the chronicle as `launched` leaves it on the first region, schedule and deck, turn 0, the city nowhere, no population. Measured: the hand settle, first worker, first scout; the centre part 37 tiles; the settle admits 24 of them, the first off the centre in the order the rules list them `-3,1`. Reached on the address, never planted.
  - _City settled, phase open_: the one above with the settle played on the centre tile, turn 0. Measured: the city's tile plain, its building the catalogue's city building, one tile held, population 1, the hand first worker, first scout. Reached on the address by the settle on screen.
  - _Bare turn 1_: `settledOn` on seed 1, the settle alone and the phase ended. Measured: one tile held and worked, population 1, idle 0, every stock 0, the culture threshold 2, six claimable tiles, the first `-1,0`, plain, yielding one food; 37 tiles charted, the nearest uncharted tile `-4,0`, coast, four out; the first bare charted tile two out with no river along it `-2,0`, coast; the first deal due on turn 6, the capstone on turn 17. The glyphs of every drawn face food 27, production 12, military 1, culture 1; of the tiles inside the border food 1, military 1, culture 1.
  - _Culture paid_: bare turn 1 with the culture threshold gained through the rules' gain helper, as trapping's production was in the worker's line, and the chronicle passed through `charted`. Measured: culture 2, everything else as bare turn 1; a claim on the first claimable tile is refused on bare turn 1 and played on this one.
  - _One tile claimed_: culture paid with the first claimable tile claimed through `apply`. Measured: two tiles held, the city's own worked and the claimed one empty, idle 0, culture 0, eight claimable tiles and the claimed one no longer among them; the drag from the city's tile onto the claimed tile answers the reassign, and the drags from the claimed tile onto the city's tile and from the city's tile onto a claimable tile both answer nothing.
  - _Worker on the city_: `settledOn` on seed 1 with the first worker played on the city's tile. Measured: the first neighbour the move command steps it onto, in the order the map lists them, `1,0`, hills.
  - _Turn before the capstone_: bare turn 1 with turns ended headlessly through `endedTurn` until the turn before the timeline's capstone turn; the chronicle not ended and no deal standing, or the seed is a finding. Measured: seed 1, the capstone due on turn 17, the fixture on turn 16, population 3, ten units on the map; the end of turn on it raises the `capstone-landing` group, deals nothing, and draws shelter, trapping, warrior, warrior, trapping.
- **What each city-mode test opens on and plays**, decided, in file order; a title changes only where named:
  - before the settle, its title now `before the settle neither the city key nor culture nor idle enters city mode`: _before the settle_ on the address, the window closed; the city key, culture and idle pressed, none entering the mode. No settle: the settle spec's second test holds the other half.
  - the city key, the ring, the threshold and the back key: _bare turn 1_; the tile is the first the city may claim, the threshold worn read through `tileCost` as now.
  - the mode letting go of the selection; the second click on the city's own tile; the right click inspecting, on the bare tile the search finds on the fixture; the press beside the tiles; culture or idle entering and the chip leaving: _bare turn 1_, the gestures as they are.
  - culture's well: _culture paid_; the well is in the accent from the opening, no turn ended; the mode entered, the first claimable tile clicked and wearing its threshold, clicked again and the claim played out; the chronicle then equals the rules' outcome of the claim on the fixture and the well is down.
  - idle's well: _bare turn 1_ on the city's own tile: clicked and ringed, clicked and unassigned — the chronicle equal to the rules' outcome of the assign, the well in the accent — clicked and assigned again, the well down. The marks counted against the chronicle's assigned tiles.
  - the marks on every tile the population stands on: _bare turn 1_ on the city's own tile, the one tile held; the paragraph that repeated the test on the city's own tile goes, being the same tile. The counts read off the fixture's assigned and held tiles.
  - the drag that carries the population, its title as it is: _one tile claimed_; the mode entered, the city's tile clicked and ringed, the drag from it onto the claimed tile; the chronicle equals the rules' reassign outcome, the assigned mark and the dim as before, the ring on the claimed tile. Nothing is unassigned on screen first: the fixture holds the empty tile.
  - the drag let go anywhere else: _one tile claimed_; the city's tile ringed; the drag from the city's tile onto the first tile the city may claim, and the drag from the claimed tile onto the city's tile; both leave the chronicle, the marks and the ring as they were.
  - the unit's tile selected lighting nothing: _worker on the city_; nothing is entered or dragged on screen; the city's tile clicked, ringed, no tile lit; the neighbour the search found clicked, ringed, no tile lit, the chronicle equal to the fixture.
  - the marks on every claimable tile: _bare turn 1_, the count from `claimable` on the fixture.
  - the second click, cut in two. The first, `a second click the city cannot pay for claims nothing and says so, and one on no act of the city's says nothing`: _bare turn 1_, the uncharted veil off through the console, the near tile the first claimable and the far tile the nearest uncharted; the near tile's note reads the `refusal.culture` text with the cost from `tileCost`, the threshold stays worn, nothing is held; the far tile rings, wears nothing, says nothing, twice. The second, `a second click the city can pay for claims the tile, which stays selected and asks for nothing more`: _culture paid_, the veil off; the near tile clicked and wearing its threshold, clicked and claimed; the chronicle equals the rules' claim outcome, the assigned marks count the outcome's assigned tiles, the claimable marks count `claimable` on the outcome and the tile is not among them, the ring stays on it with no threshold and no note; then the far tile clicked, no note.
  - the yields inside the border and of no other tile; the threshold's tile showing no glyph, on the first claimable tile; the window taking the city key: _bare turn 1_, as they are.
- **Yields keeps its seven tests on _bare turn 1_**, every number through `glyphsOf` on the chronicle's own catalogue; `withCityMode` reads the tile yields through the same lookup and never the stand-in. The test on the turn the map stands on keeps its one end of turn, the redraw being the thing tested; turn 1 ends into turn 2 with no deal due.
- **Settle keeps two tests, both on the address.** The first, whole as it is: _before the settle_ — turn 0, the city nowhere, the hand the deck's settle section read from the catalogue, the centre part drawn tile by tile, no border mark, the button reading the phase and dead — then the settle dragged out, the lit marks counted against `admitted`, the first admitted tile off the centre pressed, the city standing there with the chronicle equal to the rules' outcome of that play, the tile's building the catalogue's city building and its terrain as it was (the stand-in's `urban` goes: the Nomadic city keeps the terrain it lands on), the border marks counting the held tiles, the button's hover reading the end of the phase, the phase ended on screen, turn 1 with the phase's colour, frame and chip gone, five in hand and no settle card in any pile — the phase's end being the start it tests. The second, as it is, on _city settled, phase open_ reached by the settle on screen. The third, the free claim, is deleted: the Nomadic settle section holds no claim card, and the user chose not to rewrite it on the unit settle cards.
- **Capstone keeps two tests.** The first, as it is, on _before the settle_ on the address: the window's title, its opening lore for the capstone the first schedule names, its card, the inspection over it, the menu over it, the close, then the settle and the phase's end on screen with the window still down — that end being the one moment a second raising could slip in. The second on _turn before the capstone_ through the save opener, the opening's window closed by it: the turn ended on screen once, the window standing with the landing's lore, the card and an empty hand; the inspection over it; the close, the play-out, five in hand and the chronicle equal to the rules' outcome of the end of turn on the fixture.
- **The oracle**: after the gesture, the chronicle on screen equals the rules' outcome of the same command applied to the fixture, or equals the fixture where the gesture plays nothing, and the screen facts stay as they were — ring, threshold, note, marks, wells, frame and chip. A number read off the stand-in's tables goes; one that stays is read from the Nomadic catalogue or the chronicle through the same lookup the screen uses.
- The `budget` helper stays where a spec keeps it: city-mode ends no turn, yields one, settle one phase end, capstone one phase end and one landing.

**Traps:**

- The boot opens a new chronicle on the address only where it names a deck (`src/main.ts`): the address opener names content, seed, deck and schedule, as `openOnCapstone` builds it today on the stand-in; the region defaults to the catalogue's first. The scene's `begin` runs `launched` on those choices, so the headless twin of the address is `launched(NOMADIC, region, schedule, seed, deck)` with the firsts `firstsOf` answers, and `settledOn` is the twin of the settle on the centre tile with the phase ended. Whether `openOnCapstone` takes a catalogue or a Nomadic opener stands beside it is the implementer's call; `reference.spec.ts` still calls it on the stand-in.
- The boot reads the save only on a bare address: the save opener goes to `/`, never to an address naming content or a seed. The save is planted through `addInitScript` before the navigation; the entry key is `SAVE_ENTRY` in `src/ui/save-entry.ts`. `openSaved` closes the opening's capstone window, and the address opener leaves it standing.
- The chronicle's units list the camps' guards first, entered at the launch, and the player's worker after them: `playersOf` finds the worker, `units[0]` finds a guard.
- A rules helper called outside `apply` answers a stage, `{ chronicle }`, not a chronicle: `gained` in `src/rules/cards.ts`. The claim, assign and reassign commands go through `apply` and chart on every stage; the gain moves no tile, and the fixture is passed through `charted` in `src/rules/sight.ts` after it as the trapping fixture was. `writeSave` validates by reading back, and a fixture the reading refuses throws at the plant, which is the finding.
- The Nomadic city holds its own tile alone at the settle and its idle count is nothing (`city.idle` in `src/content/nomadic.ts`): a claim on turn 1 puts no population on the tile, idle's well fills only once the city's own tile is unassigned, and the culture threshold on turn 1 is twice one tile. The city's building yields military and culture on top of the plain it stands on, so the tiles inside the border glyph three resources and the overlay with food latched shows two more in city mode.
- The centre part is the 37 tiles within three of the centre, all charted from the settle phase, so the nearest uncharted tile stands four out and a press on it needs the uncharted veil off through the console, as the test does now. The bare charted tile two out on seed 1 is coast: the inspection reads its terrain card, and a water tile is a bare tile for the search as it stands.
- The `threshold.culture` and `refusal.culture` texts in `src/ui/text.ts` carry the number the rules give through `tileCost`; the number is never a literal.
- The chronicle screen raises the capstone's window on its first render, a resumed chronicle included (`src/ui/overlay.ts`), and its card closes it; the back key does not, being rebindable. `capstoneLore` in `src/ui/lore.ts` takes the capstone's id and the raising, `opening` or `landing`; the id is `scheduleOf(NOMADIC, schedule).capstone.id`.
- The capstone's window of turns is content: seed 1 lands it on turn 17. `endedTurn` takes first entries, which on the Nomadic content are never the do-nothing answers; seed 1 reaches turn 16 with population 3. On the capstone's turn nothing is dealt, so the end of turn on screen ends on the capstone's window alone; the enemy phase moves ten units first, so the play-out is long and the spec's budget counts it. The hand after the close holds the shelter the landing laid.
- `stoppedTurn` returns when the capstone's window stands during the play-out, and `endTurn` closes a standing one silently; the capstone's second test asserts the window itself.
- `counted` and `marksIn` read the map's scene through `window.named`, which both openers install by `readNames`; the marks are named `assigned`, `city-dim`, `claimable`, `lit`, `aim-lit`, `border`, `terrain`, `yield-<resource>` and `yield-dim`.
- CI runs the suite split by spec across four runners: press is the slowest spec and sets the slowest shard on its own; none of the four specs here comes near it.
- A spec's duration is measured with nothing else running on the machine: a run beside the user's play or another suite reads contention, not the spec. Measured today, one spec at a time, all green, three one-second Vitest runs overlapping the first: city-mode 2.0 min (eighteen tests), yields 46.3 s (seven), settle 11.5 s (three), capstone 13.2 s (two).
- The named-spec hook refuses a Playwright run that names no spec. `docs/PHASER.md` → _Under a Playwright spec_ says why a spec rests after a load, a scene start and a raise, and why a press in the frame an object appears falls through it.

**Plan:**

1. Time the four specs once each, one at a time, on an idle machine, before any file changes, and keep the durations for the report.
2. The shared module: the address opener on the Nomadic content, the fixtures — culture paid, one tile claimed, the turn before the capstone — and the tile searches on a fixture, in the shared module or in the spec that alone uses them, the implementer's call; `stepRun` and what only it calls deleted. Leaves standing: `npm run check` and `npm run lint` green, each fixture answering as measured above or a finding where one does not.
3. `e2e/settle.spec.ts`, its two tests on the address, the free claim's gone. Leaves standing: the spec green on its own run, named in the report with the question it answered.
4. `e2e/capstone.spec.ts`, its first test on the address and its second on the capstone fixture. Leaves standing: the spec green on its own run, named in the report.
5. `e2e/yields.spec.ts`, its seven tests on bare turn 1. Leaves standing: the spec green on its own run, named in the report.
6. `e2e/city-mode.spec.ts`, its nineteen tests on their fixtures, in file order. Leaves standing: the spec green on its own run, named in the report.
7. Time the four again the same way, and put both readings in the report as one table.
8. The board line deleted; `npm run check`, `npm run lint`, `npm test`.

**Verify:** `npm run check`, `npm run lint`, `npm test`. The proof spec is `npx playwright test e2e/city-mode.spec.ts`: it walks both openers, five of the fixtures, the claim, the assign and the drag against the rules' outcomes. CI proves on the push: `yields`, `settle`, `capstone`, and every other spec, since the shared module changed. The timing table: the four specs run once alone before step 2 and once after step 6, on an idle machine, durations as Playwright's list reporter prints them, both columns in the report, the free claim's deletion on its own row.
