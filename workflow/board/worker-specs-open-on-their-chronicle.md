# The worker's specs open on their chronicle

**Line:** The worker's specs open on their chronicle — building, worker-instants, harvest, rivers and inspect, reviewed the same way: each test opened on the state it needs, built headlessly on the Nomadic content, its oracle read from the rules; a worker card no age's content reaches loses its spec. Done when `e2e/worker-instants.spec.ts`, `e2e/rivers.spec.ts` and `e2e/inspect.spec.ts` import nothing from `src/content/stand-in.ts` and open through the shared save opener on the Nomadic content; `e2e/building.spec.ts` and `e2e/harvest.spec.ts` are gone; the report carries the three specs' duration before and after; CI is green.

**Spec:** `DOGMAS.md` → _Testing_, the rule **A spec opens on the save it wrote, on the game's content**, read whole, and the rule **A spec rests before it presses or measures what just changed**. The sentences the tests assert are `docs/CHRONICLE-SCREEN.md` → _The veils and the infopanel_, the paragraph on the inspected tile's cards, and `docs/MAP.md` → _The river_, unchanged; the content they read is `docs/ages/NOMADIC.md` → _The land_ and _The cards_, unchanged. No `docs/` sentence is added or changed. Player-facing sentences: none.

**Doc-impact:** none — the line changes the e2e suite alone; the rule it executes stands in `DOGMAS.md`, and no `docs/` page describes the suite.

**Scope:**

- In: `e2e/worker-instants.spec.ts`, `e2e/rivers.spec.ts`, `e2e/inspect.spec.ts`, `e2e/building.spec.ts` deleted, `e2e/harvest.spec.ts` deleted, and `e2e/chronicle-screen.ts` where a Nomadic search or fixture is shared. Out: every other spec, the stand-in catalogue, and the stand-in's helpers in the shared module — `open`, `settle`, `launch`, `workerRun`, `stepRun`, `dealRun`, `fallRun`, `atTileRun`, `unaffordableRun`, `glyphsOf` — every one of which keeps callers in the map's, the city's and the screen's specs and dies with the last of them. `riverRuns` reads no content and stays.
- **Building is deleted whole.** The farm's Nomadic counterpart, the shelter, is built through a worker in `e2e/victory.spec.ts` on the same gesture, and that spec's oracle holds the building set and the worker standing; the right click while a card is aimed leaving the aim standing, and the play then landing, is the press spec's; the map's building mark rising by one is the camps spec's, on the camps.
- **Harvest is deleted whole.** The age page rejected an instant gaining food with no worker; the two instants at nothing the age has, the camp's rewards, enter a deck only through a capture, which is the camps spec's question in the map's line. The gesture — a drag out plays a card at nothing and the chronicle equals the rules' outcome — is the press spec's on a unit card.
- **Worker-instants keeps two of its four tests, on trapping**, and its title stays. The mine's test becomes trapping improving the forest the worker stands on; the inspection test becomes the improved tile showing trapping's card between the unit's and the terrain's. The road's test goes: no age's content lays a second improvement on a tile, the refusal for the worker's spent action is held by `src/rules/cards.test.ts`, and the note on a refused tile by the refuse spec. The urbanisation test goes: no age's content terraforms through a worker card, and wildfire's burn is an event, the map's business.
- **Rivers keeps its one test.** Its fixture is a bare settle whose opening charts a river; the end turn stays on screen, being the thing tested: the redraw of a turn leaves each run painted once, none doubled, none left over. Measured: seed 2 charts two runs, and 153 of the first 300 seeds chart one.
- **Inspect keeps all five tests**, each on a bare settle found by its own predicate, the predicates as they stand rewritten on the Nomadic catalogue, the deck being the Nomadic deck:
  - the feature's mark and its row: the first seed with a feature on a neighbour of the city. Measured: seed 3, fertile at `-1,0`.
  - the river's row: the first seed with a river along a neighbour of the city that is bare of feature and of a terrain the river feeds. Measured: seed 3, plain at `0,-1`. The tile holds that one card, so the further press leaves it standing, as now.
  - the movement cost and the dash: the first seed with a neighbour costing two move points, bare, nobody on it, and a water tile among the opening's snapshots. Measured: seed 1, the land at `1,0`, coast at `-3,0`; 202 of the first 300 seeds. The cost the card reads is the tile's, `movementCost` over `MOVE_POINT`, never the literal.
  - the cycle and the back key: the settle with the first worker played on the city's tile at the settle, the tile west of the city bare and no river along the city's tile or the west one, as `stepsClear` asks. Measured: seed 1. Nothing is dragged on screen: the worker stands on the city from the save, and the city's tile carries its three cards, unit, building, terrain, in that order.
  - the right click: the first seed with a bare neighbour. Measured: seed 1, `1,0`.
- **Two fixtures for worker-instants**, one built on the other:
  - _Worker on a forest, trapping paid_: the shared `workerStepped` search — the settle with the first worker played on the city's tile, the worker moved through the move command onto the first neighbour where the predicate holds — with the predicate that the neighbour is forest and trapping stands in the hand; then trapping's cost gained through the rules' gain helper, as the victory fixture gained the shelter's cost, and the chronicle passed through `charted`. Trapping then admits the worker's tile: forest, unimproved, a worker with its action on it. Measured: seed 3, the forest at `1,0`, the hand gather, gather, warrior, march, trapping, the stock production 2 and nothing else; 130 of the first 300 seeds answer.
  - _Trapping laid_: the one above with trapping played through `apply` at the worker's tile, headlessly. The worker still stands there, its action spent, and the tile's improvements are trapping alone.
- **What each worker-instants test opens on and plays**, decided:
  - trapping improves the forest: _worker on a forest_; the improvement marks counted, trapping dragged out and aimed at the worker's tile, the play waited out. The chronicle on screen equals the rules' outcome of trapping played at that tile on the fixture — the improvement laid, the production paid, the worker standing — and the map shows one improvement mark more.
  - the improved tile inspects the improvement card between the unit's and the terrain's: _trapping laid_; the tile clicked and ringed, the inspection key stepping unit, then the building card headed by trapping with one row, then terrain. The row reads trapping's name as the text table gives it and its food from `improvementKind` on the Nomadic catalogue, as the mine's did off the stand-in.
- **The oracle**: after the gesture, the chronicle on screen equals the rules' outcome of the same command applied to the fixture, or equals the fixture where the gesture plays nothing, and the screen facts stay as they were — ring, inspection, marks. A number read off the stand-in's tables goes; one that stays is read from the Nomadic catalogue through the same lookup the screen uses.
- A search answers the chronicle it found and whatever the spec needs beside it — the tile's key, the feature, the terrain — never a seed to replay through the screen.
- The `budget` helper stays where a spec keeps it; rivers counts its one end of turn, and nothing else in the three specs ends a turn.

**Traps:**

- The boot reads the save only on a bare address (`src/main.ts`): the opener goes to `/`, never to an address naming content or a seed. The save is planted through `addInitScript` before the navigation; the entry key is `SAVE_ENTRY` in `src/ui/save-entry.ts`. The shared module already holds the opener, `openSaved`, the Nomadic fixture builder, `settledOn`, the seed search, `firstSeed`, the stepped-worker search, `workerStepped`, and the headless turn, `endedTurn`; every one takes the catalogue from the chronicle or as an argument.
- A rules helper called outside `apply` charts nothing: the gain helper leaves the snapshots as the last command left them. The fixture is passed through `charted` in `src/rules/sight.ts` after it, as the victory fixture is; `writeSave` validates by reading back, and a fixture the reading refuses throws at the plant, which is the finding.
- `gained` in `src/rules/cards.ts` answers a stage, `{ chronicle }`, not a chronicle; the victory spec shows the call.
- The Nomadic city holds its own tile alone at the settle, and its settle centre is plain on every seed: the city yields no production, so a turn-1 stock cannot pay trapping without the gain, and every neighbour lies outside the border.
- `throughWorker` in `src/rules/cards.ts` refuses `no-worker` before the tile's own reasons: trapping admits only a tile a worker with action stands on. The worker stepped onto forest has spent both move points and none of its action.
- The chronicle screen raises the capstone's window on its first render, a resumed chronicle included (`src/ui/overlay.ts`); its card closes it, and the back key does not, being rebindable. Every fixture here is turn 1, before the first deal.
- The infopanel names an improvement's card `building`, headed by the improvement where no building stands (`shownCard` answers `building` for it now, on the mine); the row's chip is named `panel-yield-<resource>`.
- `inspect.spec.ts` reads the feature's name through `featureName` and the river's through the `panel.river` text; both are content-free and stay. The movement cost's text is `panel.movement` with `cost`, the dash `panel.no-movement`.
- A river along a tile is a row of the terrain card, not a card of its own: `stepsClear` excludes rivers so the west tile's cycle is one card and the city's three.
- `counted` and `marksIn` read the map's scene through `window.named`, which `openSaved` installs by `readNames`.
- CI runs the suite split by spec across four runners: press is the slowest spec and sets the slowest shard on its own; none of the three specs here comes near it.
- A spec's duration is measured with nothing else running on the machine: a run beside the user's play or another suite reads contention, not the spec. Measured today, one spec at a time, all green: building 44.9 s, worker-instants 1.6 min, harvest 11.8 s, rivers 10.4 s, inspect 53.9 s.
- The named-spec hook refuses a Playwright run that names no spec. `docs/PHASER.md` → _Under a Playwright spec_ says why a spec rests after a load, a scene start and a raise, and why a press in the frame an object appears falls through it.

**Plan:**

1. `e2e/building.spec.ts` and `e2e/harvest.spec.ts` deleted. Leaves standing: `npm run check` and `npm run lint` green, nothing importing them.
2. The Nomadic searches and fixtures, in the shared module or in the spec that alone uses them, the implementer's call: the river charted, the feature beside the city, the river's fed neighbour, the cost and the dash, the west tile clear, the bare neighbour, the worker on a forest with trapping paid, trapping laid. Leaves standing: each search answering on the seeds measured above, or a finding where one does not.
3. `e2e/rivers.spec.ts`, its one test on its fixture. Leaves standing: the spec green on its own run, named in the report with the question it answered.
4. `e2e/worker-instants.spec.ts`, its two tests on their fixtures, the road's and urbanisation's gone, `moveOut` and `roadLands` with them. Leaves standing: the spec green on its own run, named in the report.
5. `e2e/inspect.spec.ts`, its five tests on their fixtures, in file order. Leaves standing: the spec green on its own run, named in the report.
6. Time the three again the same way, and put both readings in the report as one table, with building's and harvest's before readings on rows of their own, marked deleted.
7. The board line deleted; `npm run check`, `npm run lint`, `npm test`.

**Verify:** `npm run check`, `npm run lint`, `npm test`. The proof spec is `npx playwright test e2e/inspect.spec.ts`: it walks five fixtures, the opener, the searches on the map, the infopanel's cards and the ring. CI proves on the push: `worker-instants`, `rivers`, and every other spec, since the shared module changed. The timing table: the three specs run once alone before step 1, the readings above, and once after step 5, on an idle machine, durations as Playwright's list reporter prints them, both columns in the report.
