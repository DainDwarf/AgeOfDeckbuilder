# One catalogue

**Line:** One catalogue — `src/content/` exports one catalogue, no list of them and no lookup by version; the launch page has no content row and the boot reads no `content` key; the save is read against the one catalogue; its version is `1`; no file outside `src/content/` imports the age's module. Done when `npm run check`, `npm run lint` and `npm test` are green, the searches under _Verify_ find nothing, and CI is green.

**Spec:** `DOGMAS.md` → _Architecture_, the rule **The catalogue is an argument**, its sentence _There is one catalogue, and an age is a slice of it in two halves_ — this line lands the first clause; the halves and the merge are the next line's — and the rule **A catalogue is validated once, when it is built, and a chronicle names its catalogue's version**, unchanged: `apply` and the save's reading refuse a chronicle of another version as they do today, through the one catalogue. `docs/META.md` → _The save_, unchanged: a save naming a version the game no longer ships is refused at the boot, dropped, the reason on the browser's console alone. `docs/INTERFACE.md` → _The launch page_, two sentences change: _The page is one row per choice — the content, the region, the schedule, the deck and the seed — the first of each list chosen until another is pressed_ becomes _The page is one row per choice — the region, the schedule, the deck and the seed — the first of each list chosen until another is pressed_, and the sentence _The rows list the ages' content alone._ is deleted. `docs/INTERFACE.md` → _A failed boot_, one sentence changes: _or the address names content the game does not hold_ becomes _or the address names what the content does not hold_. Three sentences outside `docs/` change in the same commit, written out here so none is authored: `.claude/skills/run/SKILL.md` step 5 reads _**Say what the address opens.** The bare address, no option, is the normal door: it boots on the save — the chronicle in progress where it stood, else the launch page. The chronicle screen writes nothing into the address. Naming a deck opens a chronicle straight and writes it over the save (`?deck=nomadic`)._; `.claude/agents/ui-check.md` → _What the app is_, its second paragraph's list reads _`?seed=<integer>` picks the map, `?deck=` the cards, and the same URL opens the same chronicle every time_. The layout row of `DOGMAS.md` → _Stack_ already reads _the catalogue: one module per age, merged into the one the boot hands the rules and the screen_; this line makes its first half true and leaves the merge to the next. Player-facing sentences: none; the launch page's _Content_ label leaves the text table with its row, and nothing is added.

**Doc-impact:** `docs/INTERFACE.md` → _The launch page_ and _A failed boot_, the three sentences above.

**Scope:**

- In: `src/content/catalogues.ts`, which becomes the one catalogue's module; `src/content/nomadic.ts` for its version; `src/main.ts`, `src/ui/launch-page.ts`, `src/ui/text.ts`, `src/ui/save-entry.ts`, `src/ui/chronicle-scene.ts` where they take the catalogue; `src/rules/save.ts` and `src/rules/save.test.ts` for the version reader; `e2e/chronicle-screen.ts`, `e2e/boot.spec.ts`, `e2e/hover.spec.ts` and every spec that imports the age's module; the three `docs/` sentences and the three harness sentences. Out: the ages, the slice and the merge (the next line); the city (the line after); the launch page's other rows and its layout; `src/content/nomadic.test.ts`, which stays the age's test until the merge makes it the catalogue's; `CHANGELOG.md`, being history; `e2e/failed-boot.spec.ts`, which fails the boot on WebGL and names no content.
- **The one catalogue** is what `src/content/` exports, from one module, under one name; the boot, the launch page, the save entry, the chronicle scene and the specs take it from there and nowhere else, and the rules keep receiving it as an argument. The list of catalogues and the lookup by version go. Its version is the string `1`, declared where the catalogue literal is built; the fixture's stays `fixture`.
- **The address** reads `region`, `schedule`, `deck` and `seed`; `content` is no longer a key it reads, so an address naming `content` and nothing else is bare and boots on the save, and one naming `content` beside a deck opens the chronicle and ignores it. An address naming a deck, a region or a schedule the catalogue does not hold still throws before Phaser and shows the failed-boot page, as today.
- **The launch page** has four rows — region, schedule, deck, seed — and Launch; the content row, its faces named `launch-content-<version>`, its choose branch and its text key go.
- **The save** is read against the one catalogue: the boot hands the text to the save's reading, which refuses a chronicle of another version by itself, as it does today; the reader that pulled the version out of the text before any catalogue was known goes, and so does its test, since what it promised — a save names the content it was written on, and another version is refused — the reading's own test still promises. A dropped save still says why on the console, the message opening with the catalogue's version.
- **The specs**: every spec that imports the age's module imports the one catalogue's module instead, so the next line, which turns the age's module into a slice, sweeps no spec. The shared helper's lookups by the chronicle's version become the one catalogue. The address opener names the seed, the deck and the schedule. The boot spec's first test names the deck alone beside the version it reads from the one catalogue; its test that the launch page lists the ages' content alone is deleted with the row; its launch test reads the chronicle's content against the one catalogue's version.
- **The one corner**: the save standing in a browser today names `nomadic`; the first boot on this line refuses it, drops it, and the console says so. The hand-back says this to the user, whose chronicle in progress it is.

**Traps:**

- The address is read at the top of `src/main.ts`, before Phaser boots, and `bare` — no key the boot reads named — is what decides whether the save is read at all; a key removed from the list the boot reads makes an address naming it bare.
- Every refusal of the content opens with the catalogue's version (`refuse` in `src/rules/map-kinds.ts`); the rules tests match `/^fixture: /` on the fixture and nothing matches `/^nomadic: /` today, so the version `1` changes no assertion, only the console's words.
- `src/rules/` never imports `src/content/` (`DOGMAS.md` → _Stack_): the one catalogue's module lives in `src/content/`, reached from `src/ui/`, `src/main.ts` and `e2e/` alone; the save's reading in `src/rules/save.ts` keeps taking the catalogue as an argument.
- The text table's key type is derived from its entries (`src/ui/text.ts`), and the launch page builds its label keys from its row names; a row removed from one and not the other fails the typecheck, which is the check working.
- The specs import the age's module in some thirty files, `e2e/chronicle-screen.ts` resolving the catalogue from the chronicle's version in eight places besides; the sweep is mechanical and the done-condition's search proves it whole.
- The first boot after this line drops the user's chronicle in progress, since its save names `nomadic`; nothing in the code can keep it, and nothing should.
- CI runs the suite split by spec across four runners; the boot spec runs in seconds. `docs/PHASER.md` → _Under a Playwright spec_ says why a spec rests after a load and a scene start; the boot spec's tests rest already, and this line adds no press.

**Plan:**

1. The one catalogue's module in `src/content/`, its version `1`, the list and the lookup deleted; the boot, the launch page, the save entry and the chronicle scene take the catalogue from it; the `content` key leaves the address; the content row and its text key leave the launch page; the version reader and its test leave the save, the save entry reading through the save's reading on the one catalogue. Leaves standing: `npm run check`, `npm run lint` and `npm test` green.
2. The specs: every import of the age's module outside `src/content/` names the one catalogue's module; the shared helper's lookups by version become the one catalogue; the address opener drops `content`; the boot spec's first test's address, its content-row test deleted, its launch test on the one catalogue's version. Leaves standing: the boot spec green on its own run, named in the report.
3. The three `docs/` sentences and the three harness sentences as written under _Spec_. Leaves standing: `npm run lint` green.
4. The board line deleted.

**Verify:** `npm run check`, `npm run lint`, `npm test`; a search over `src/` and `e2e/` for `catalogueOf`, `CATALOGUES` and `content=` finding nothing, and for imports of `content/nomadic` finding none outside `src/content/`. The proof spec is `npx playwright test e2e/boot.spec.ts`: it walks the address naming a deck, the bare address on no save, the console over the launch page and Launch on the defaults. CI proves on the push: `resume`, `hover`, `failed-boot`, and every other spec, since the shared helper changed.
